import cheerio from "cheerio";

const autotrim88Strategy = async (fullText) => {
  try {
    if (fullText.length > 0) {
      // const test = $(".d-item-description");
      // console.log("test");
      // console.log(test);
      return "";
    } else {
      return "";
    }
  } catch (error) {
    console.log("error from autotrim88Strategy");
    console.log(error);
    return "";
  }
};

export default autotrim88Strategy;
